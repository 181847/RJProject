package unfinishedClass.customRClass.rStruct;

/**
 * 可以用来存储脚本定义的结构，
 * 与这种结构相对应的还有一种RSet，
 * RSet可以存储一系列相同类型的RStruct和RSet（相当于数组），
 * 而RStruct用于存储不同类型的RStruct和RSet（相当于类的成员变量）。
 * @param <T>
 * 		这里的T被限制为RStruct的子类。
 */
public interface RStruct {

}
