package unfinishedClass.customRClass.struct;

/**
 * Function的Parameter（参数）组件信息，
 * 详细信息包括：
 * 参数组件的类型，
 * 参数组件的名字，
 * 参数组件的基本数据类型的初始化值。
 */
public class ParameterStruct extends VarStruct {
	public ParameterStruct(String parameterInfo){
		super(parameterInfo);
	}
}
