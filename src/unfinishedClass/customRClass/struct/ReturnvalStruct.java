package unfinishedClass.customRClass.struct;

/**
 * Function的Returnval（返回值）组件信息，
 * 详细信息包括：
 * 返回值组件的类型，
 * 返回值组件的名字，
 * 返回值组件的基本数据类型的初始化值。
 */
public class ReturnvalStruct extends VarStruct {
	public ReturnvalStruct(String returnvalInfo){
		super(returnvalInfo);
	}
}
